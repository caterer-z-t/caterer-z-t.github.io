# Personal Website

## Preview site before pushing
You’ll need Ruby + Bundler. Since you already have a `Gemfile`, you can use Bundler to install dependencies:
```bash 
gem install bundler jekyll
bundle install
```


From inside repo
```bash
bundle exec jekyll serve
```